<%if Rails.env == 'production'%>
    var disqus_shortname = 'production-cityspade'; // required: replace example with your forum shortname
<% else %>
    var disqus_shortname = 'cityspade'; // required: replace example with your forum shortname
<% end %>
var meta_url = jQuery('meta[property="disqus:url"]');
if(meta_url.length == 0){
    var disqus_url = meta_url.attr('content')
  }else{
    var disqus_url = window.location.href.split('?')[0].split('-')[0];
  }
/* * * DON'T EDIT BELOW THIS LINE * * */
(function() {
  var dsq = document.createElement('script'); dsq.type = 'text/javascript'; 
  dsq.async = true;
  dsq.src = '//' + disqus_shortname + '.disqus.com/embed.js';
  (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
})();
